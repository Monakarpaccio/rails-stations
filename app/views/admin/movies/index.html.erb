<!DOCTYPE html>
<html>
<head>
  <title>Movies List</title>
</head>
<body>
  <h1>Movies</h1>
  <table>
    <% @movies.each do |movie| %>
      <h2><%= movie.name %></h2>
      <p>Year: <%= movie.year %></p>
      <p>Description: <%= movie.description %></p>
      <p><img src="<%= movie.image_url %>" alt="<%= movie.name %> image"></p>

      <% if movie.schedules.any? %>
        <ul>
          <% movie.schedules.each do |schedule| %>
            <li>
              <p>開始時刻: <%= schedule.start_time %></p>
              <p>終了時刻: <%= schedule.end_time %></p>
              <%= link_to '詳細', admin_schedule_path(schedule) %> |
              <%= link_to '編集', edit_admin_schedule_path(schedule) %> |
              <%= link_to '削除', admin_schedule_path(schedule), method: :delete, data: { confirm: '削除してもよろしいですか？' } %>
            </li>
          <% end %>
        </ul>
      <% end %>
    <% end %>
  </table>
</body>
</html>
